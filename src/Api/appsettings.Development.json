{
  "Logging": {
    "LogLevel": {
      "Default": "Debug",
      "Microsoft.AspNetCore": "Information"
    }
  },
  "_comment_GlobalConfig": "GlobalConfig:Path and Observability:LogFile:Directory are machine-specific — set GlobalConfig:Path in your own gitignored appsettings.Local.json (see appsettings.Local.json.example), and put Observability:LogFile:Directory inside your globalconfig.json alongside the other per-machine values. ConnectionStrings:UserDb and Mongo:ConnectionString have moved there too — see globalconfig.json.",
  "Mongo": {
    "DatabaseName": "kart_user",
    "CollectionName": "user_read_model"
  },
  "RabbitMq": {
    "HostName": "localhost",
    "ManifestPath": "message-bus-manifest.json"
  },
  "Jwt": {
    "Issuer": "kart-identity-service",
    "JwksUri": "http://localhost:5001/.well-known/jwks.json"
  }
}
